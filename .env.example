# Vuhlp provider transports: use "cli" for local CLIs (preferred for true stateful sessions),
# or set to "api" to use native adapters.
VUHLP_CLAUDE_TRANSPORT=cli
VUHLP_CODEX_TRANSPORT=cli
VUHLP_GEMINI_TRANSPORT=cli

# Daemon bind host/port (optional)
# VUHLP_BIND_HOST=0.0.0.0
# VUHLP_PORT=4000

# Mobile app - Development: set packager hostname for device testing
# REACT_NATIVE_PACKAGER_HOSTNAME=your-device-routable-host

# Mobile app - Production: set full API URL (use HTTPS)
# EXPO_PUBLIC_API_URL=https://api.yourdomain.com

# Claude (Anthropic)
VUHLP_CLAUDE_API_KEY=your-anthropic-key
VUHLP_CLAUDE_MODEL=claude-3-5-sonnet-latest
VUHLP_CLAUDE_API_URL=https://api.anthropic.com
VUHLP_CLAUDE_MAX_TOKENS=2048

# Codex (OpenAI)
VUHLP_CODEX_API_KEY=your-openai-key
VUHLP_CODEX_MODEL=gpt-4o
VUHLP_CODEX_API_URL=https://api.openai.com/v1
VUHLP_CODEX_MAX_TOKENS=2048

# Gemini (Google)
VUHLP_GEMINI_API_KEY=your-gemini-key
VUHLP_GEMINI_MODEL=gemini-1.5-pro-latest
VUHLP_GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta
VUHLP_GEMINI_MAX_TOKENS=2048

# Gemini CLI (local fork with stream-json input)
# VUHLP_GEMINI_COMMAND=/absolute/path/to/gemini
# Gemini defaults to native tool execution; add --core-tools none if you want Gemini CLI to disable native tools (use tool_call JSON for vuhlp tools).
# VUHLP_GEMINI_ARGS=--core-tools none
